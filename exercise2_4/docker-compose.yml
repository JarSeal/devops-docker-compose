version: '3.5'

services:

    backend:
      build:
        context: ./backend
      ports:
        - 8080:8080
    frontend:
      build:
        context: ./frontend
      ports:
        - 5000:5000
    redis:
      image: redis
      ports:
        - 6379:6379